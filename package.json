{
    "private": true,
    "name": "star-wars",
    "workspaces": [
        "packages/client",
        "packages/server"
    ],
    "scripts": {
        "test:e2e": "playwright test",
        "test:unit": "yarn workspaces foreach run test:ci"
    },
    "devDependencies": {
        "@playwright/test": "^1.34.3",
        "lint-staged": "^13.2.2"
    }
}
